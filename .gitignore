# 中間生成物(aggregate.py が作り、build_payload.py が読む)
agg.json

# 元データは置かない
*.xlsx
~$*
# 統合済みの生データ。顧客名・電話・住所を含む。リポジトリは public なので絶対にコミットしない
raw_orders.json

__pycache__/
*.pyc
.vercel
.DS_Store

# APIトークン。絶対にコミットしない
.env
.env.*
orders.json
raw_response.json

# 販売記録スプレッドシートの作業ファイル。顧客名・住所・電話を含むので絶対に入れない
sheet_raw.txt
sheet.json
region_map.json
unmatched.json

# From 欄の人名判定リスト(公開しない)
regions_staff.txt

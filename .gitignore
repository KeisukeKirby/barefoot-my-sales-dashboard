# 中間生成物(aggregate.py が作り、build_payload.py が読む)
agg.json

# 元データは置かない
*.xlsx
~$*
# 統合済みの生データ。顧客名・電話・住所を含む。リポジトリは public なので絶対にコミットしない
raw_orders.json

__pycache__/
*.pyc
.vercel
.DS_Store

# APIトークン。絶対にコミットしない
.env
.env.*
orders.json
raw_response.json
